# ============================================
# INSTANT WIN LOTTERY - BACKEND CONFIGURATION
# ============================================

# Server
PORT=3001
NODE_ENV="development"

# ============================================
# DATABASE
# ============================================
#
# DEVELOPMENT (SQLite):
#   1. Copy schema.sqlite.prisma to schema.prisma:
#      cp prisma/schema.sqlite.prisma prisma/schema.prisma
#   2. Use SQLite URL:
DATABASE_URL="file:./dev.db"
#
# PRODUCTION (PostgreSQL):
#   1. Use the default schema.prisma (already configured for PostgreSQL)
#   2. Set your PostgreSQL connection string:
# DATABASE_URL="postgresql://username:password@hostname:5432/database_name?sslmode=require"
#
# Railway PostgreSQL Example:
# DATABASE_URL="postgresql://postgres:password@containers-xxx.railway.app:5432/railway"

# ============================================
# SECURITY
# ============================================
# IMPORTANTE: Generate a strong random secret for production!
# Use: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-this"

# ============================================
# URLS
# ============================================
# Frontend URL (for QR codes and CORS)
FRONTEND_URL="http://localhost:3000"

# Backend URL (for API links in PDFs)
# BACKEND_URL="http://localhost:3001"

# ============================================
# SUPER ADMIN (optional - can also seed)
# ============================================
# SUPERADMIN_USERNAME="superadmin"
# SUPERADMIN_EMAIL="admin@instantwin.io"
# SUPERADMIN_PASSWORD="change-this-password"

# ============================================
# EMAIL (SMTP)
# ============================================
# Required for password reset and notifications
# Leave empty to disable email functionality
#
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"
# SMTP_FROM="noreply@instantwin.io"
#
# For Gmail: Use App Passwords (https://myaccount.google.com/apppasswords)
# For Resend: Use api key as password, smtp.resend.com as host

# ============================================
# FILE UPLOADS
# ============================================
# Maximum file size in bytes (default: 5MB)
MAX_FILE_SIZE="5242880"
# Allowed file types
ALLOWED_FILE_TYPES="image/jpeg,image/png,image/gif,image/webp,image/svg+xml"
# Upload directory (relative to backend root)
UPLOAD_DIR="uploads"
