# Root .env - Required for running commands from project root
# Copy this file to .env: cp .env.example .env
# Backend-specific settings are in backend/.env

# Database URL for Prisma (SQLite for local development)
DATABASE_URL="file:./backend/dev.db"
